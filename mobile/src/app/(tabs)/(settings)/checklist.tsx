// The checklist template editor, mounted in the Settings stack as well as
// Home's — the run screen's pencil resolves group-relative, so an edit begun
// from Settings stays (and returns) there.
export { default } from '../(dashboard)/checklist';
