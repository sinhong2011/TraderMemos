// The Daily checklist run, mounted in the Settings stack as well as Home's —
// reached from Trading & journal, back must land back there, and a push into
// the Home stack would pop to Home instead. Same screen, one implementation.
export { default } from '../(dashboard)/daily-checklist';
