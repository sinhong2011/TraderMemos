// Tools-menu entry point; the screen lives in components/symbol-journal.tsx
// because the (trades) stack mounts the same screen from a trade's symbol chip.
export { default } from '@/components/symbol-journal';
